<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.market.repository.mapper.UserMapper">
	<insert id="insertUser" parameterType="UserDto">
		INSERT INTO USER2 (email, password, name, tel)
		VALUES (#{email}, #{password}, #{name}, #{tel})
	</insert>
	<select id="findByEmail" resultType="UserDto">
		SELECT id, email, password, name, tel
		FROM USER2
		WHERE email = #{email}
	</select>
	<select id="findByEmailAndPassword" resultType="UserDto">
		SELECT id, email, password, name, tel
		FROM USER
		WHERE email = #{email}
		  AND password = #{password}
	</select>
</mapper>
